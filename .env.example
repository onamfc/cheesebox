# ========================================
# REQUIRED ENVIRONMENT VARIABLES
# ========================================

# Database
# For Railway/Vercel deployment, use your Railway PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/private_video?schema=public"

# NextAuth Configuration
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Optional - for Google Sign-In)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Set authorized redirect URI to: http://localhost:3000/api/auth/callback/google (and production URL)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# JWT Secret for mobile authentication and streaming tokens
# CRITICAL SECURITY: Generate a strong secret with: openssl rand -base64 64
# REQUIRED: Application will fail to start if not set
JWT_SECRET="your-jwt-secret-change-this-in-production"

# Encryption key for AWS credentials stored in database
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="your-encryption-key-change-this-in-production"

# CSRF Protection Secret
# Generate with: openssl rand -base64 32
# REQUIRED: Used to hash CSRF tokens for security
CSRF_SECRET="your-csrf-secret-change-this-in-production"

# Application URLs
NEXT_PUBLIC_APP_URL="http://localhost:3000"
LINKFORTY_BASE_URL="https://go.cheesebox.io"

# ========================================
# STRONGLY RECOMMENDED (Security)
# ========================================

# Upstash Redis (for rate limiting)
# WITHOUT THESE: Rate limiting is DISABLED - vulnerable to brute force attacks
# WITH THESE: Login limited to 5 attempts/15min, Registration limited to 3/hour
# Get FREE tier at: https://console.upstash.com/ (500k commands/month)
UPSTASH_REDIS_REST_URL="your-upstash-redis-url"
UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

# ========================================
# OPTIONAL CONFIGURATION
# ========================================

# Pre-signed URL expiry (in seconds)
# Default: 10800 (3 hours)
# PRESIGNED_URL_EXPIRY="10800"

# ========================================
# EMAIL CONFIGURATION
# ========================================
# NOTE: Email credentials are configured in the Settings UI, not here!
# Each user/team configures their own email provider (Resend, AWS SES, or SMTP)
# in the application Settings page. Credentials are encrypted in the database.
#
# Supported providers:
# - Resend (recommended for simplicity)
# - AWS SES (low cost, high volume)
# - SMTP (Gmail, Outlook, custom servers)
#
# No environment variables needed for email!

# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
